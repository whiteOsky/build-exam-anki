# 第1章 进程状态与转换

## 三种基本状态

- **运行态**：进程正在处理机上运行。
- **就绪态**：进程已经具备除处理机以外的全部运行条件，只等待获得处理机。
- **阻塞态**：进程因等待资源或事件而暂时不能继续运行，即使获得处理机也无法执行。
- **易错边界**：就绪态缺少的是处理机；阻塞态缺少的是等待中的资源或事件完成条件。

## 状态转换

- 运行态进程请求尚未满足的资源或等待事件时，转为阻塞态。
- 等待的资源可用或事件完成后，阻塞态转为就绪态。
- 阻塞态不能直接转为运行态，必须先进入就绪态并经调度获得处理机。
